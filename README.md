This project implements a fully synthesizable 3x3 matrix logic core and automated processing engine designed entirely in Verilog HDL inside Xilinx Vivado to execute deterministic, real-time game referee operations on physical silicon fabric. Shifting away from slow, software-driven processors or multi-cycle sequential loops, the micro-architecture consolidates state machine transitions, cell checking, and result capture into a highly optimized synchronous pipeline (posedge clk) that maps onto an absolute hardware footprint of just 41 Slice LUTs and 19 Registers under a total power envelope of 0.793W when fully targeted to an Artix-7 FPGA chip. The core logic handles user command pacing via a rigid streaming handshake protocol (Valid/Ready), natively validates cell occupancy inline to route illegal overwrite attempts into a 1-cycle diagnostic collision pulse (o_illegal_move), and leverages an explicit combinational reduction gate matrix to evaluate all eight winning vectors simultaneously in a single clock cycle. The structural integrity and clock domain boundaries of the hardware chip were fully validated using an independent, self-checking verification testbench that automatically verified exact functional victory parameters and security lockout thresholds under a cycle-accurate behavioral simulation runtime of 127.000 ns with zero timing endpoint failures.Would you like me to output the exact terminal text block to push this project folder directly to GitHub or provide the top technical interview questions design managers will ask you about this synchronous logic pipeline?Generate Git terminal commandsReview Interview questions
